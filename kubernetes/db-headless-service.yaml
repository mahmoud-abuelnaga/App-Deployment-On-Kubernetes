apiVersion: v1
kind: Service
metadata:
  name: db-headless-service
  labels:
    app: color-api
    tier: service

spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: color-api
    tier: database

  ports:
    - port: 27017
      targetPort: 27017
